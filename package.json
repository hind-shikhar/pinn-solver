{
  "name": "pinnsolver",
  "version": "1.0.0",
  "description": "Browser-based ODE/PDE solver using Physics-Informed Neural Networks (PINNs)",
  "main": "index.html",
  "scripts": {
    "start": "npx serve . --listen 3000 --single",
    "dev": "npx serve . --listen 3000",
    "deploy:vercel": "npx vercel --prod",
    "deploy:netlify": "npx netlify deploy --prod --dir ."
  },
  "keywords": [
    "pinn",
    "pde",
    "ode",
    "neural-network",
    "tensorflow",
    "physics"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@tensorflow/tfjs": "^4.22.0"
  }
}
